/**
 * The one title string used everywhere a role is named: homepage subtitle,
 * default <title>, Person schema jobTitle. Decided in MTC-24; change it here
 * only; the social card (lib/og/card.tsx) reads it at build time.
 */
export const JOB_TITLE = 'Engineering Leader & Agentic Engineer'

/** The homepage's one-line pitch; the social card repeats it. */
export const TAGLINE = 'I build software products, and level up teams.'
